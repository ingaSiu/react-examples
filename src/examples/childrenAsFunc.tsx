// Render props => kai daliname koda tarp react components, naudodami prop, kurio verte yra funkcija

// formiko naudoja children as a function

const Examples = () => {
  return (
    <>
      {/* <Formik>
        {({ value, handleSubmit, handleReset }) => (
          <Form>
            <p>hi there</p>
          </Form>
        )}
      </Formik> */}
    </>
  );
};

export default Examples;
